# OperationEnum

Specifies the operation to perform. Use `add` to associate or `remove` to disassociate.

## Example Usage

```typescript
import { OperationEnum } from "@cloudinary/account-provisioning";

let value: OperationEnum = "add";
```

## Values

```typescript
"add" | "remove"
```