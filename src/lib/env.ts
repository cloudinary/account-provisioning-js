/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: c52972a3b198
 */

import * as z from "zod/v4-mini";
import { SDKOptions } from "./config.js";
import { dlv } from "./dlv.js";

export interface Env {
  CLOUDINARY_PROVISIONING_API_KEY?: string | undefined;
  CLOUDINARY_PROVISIONING_API_SECRET?: string | undefined;

  /**
   * Sets the accountId parameter for all supported operations
   */
  CLOUDINARY_ACCOUNT_ID?: string | undefined;

  CLOUDINARY_DEBUG?: boolean | undefined;
}

export const envSchema: z.ZodMiniType<Env, unknown> = z.object({
  CLOUDINARY_PROVISIONING_API_KEY: z.optional(z.string()),
  CLOUDINARY_PROVISIONING_API_SECRET: z.optional(z.string()),

  CLOUDINARY_ACCOUNT_ID: z.optional(z.string()),

  CLOUDINARY_DEBUG: z.optional(z.coerce.boolean()),
});

/**
 * Checks for the existence of the Deno global object to determine the environment.
 * @returns {boolean} True if the runtime is Deno, false otherwise.
 */
function isDeno() {
  if ("Deno" in globalThis) {
    return true;
  }

  return false;
}

let envMemo: Env | undefined = undefined;
/**
 * Reads and validates environment variables.
 */
export function env(): Env {
  if (envMemo) {
    return envMemo;
  }

  let envObject: Record<string, unknown> = {};
  if (isDeno()) {
    envObject = (globalThis as any).Deno?.env?.toObject?.() ?? {};
  } else {
    envObject = dlv(globalThis, "process.env") ?? {};
  }

  envMemo = envSchema.parse(envObject);
  return envMemo;
}

/**
 * Clears the cached env object. Useful for testing with a fresh environment.
 */
export function resetEnv() {
  envMemo = undefined;
}

/**
 * Populates global parameters with environment variables.
 */
export function fillGlobals(options: SDKOptions): SDKOptions {
  const clone = { ...options };

  const envVars = env();

  if (typeof envVars.CLOUDINARY_ACCOUNT_ID !== "undefined") {
    clone.accountId ??= envVars.CLOUDINARY_ACCOUNT_ID;
  }

  return clone;
}
