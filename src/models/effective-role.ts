/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  EffectivePolicy,
  EffectivePolicy$inboundSchema,
} from "./effective-policy.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type EffectiveRole = {
  /**
   * The unique identifier of the role.
   */
  id?: string | undefined;
  /**
   * A list of all the predefined permission policies that are associated with this role.
   */
  systemPolicies?: Array<EffectivePolicy> | undefined;
};

/** @internal */
export const EffectiveRole$inboundSchema: z.ZodMiniType<
  EffectiveRole,
  unknown
> = z.pipe(
  z.object({
    id: types.optional(types.string()),
    system_policies: types.optional(z.array(EffectivePolicy$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "system_policies": "systemPolicies",
    });
  }),
);

export function effectiveRoleFromJSON(
  jsonString: string,
): SafeParseResult<EffectiveRole, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EffectiveRole$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EffectiveRole' from JSON`,
  );
}
