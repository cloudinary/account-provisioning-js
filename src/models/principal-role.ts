/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6b75b54cbe68
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  EffectiveRole,
  EffectiveRole$inboundSchema,
} from "./effective-role.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type PrincipalRole = {
  /**
   * The type of principal you want to retrieve roles or permission policies for.
   *
   * @remarks
   *
   * - user
   *
   * - group
   *
   * - apiKey
   *
   * - provisioningKey
   */
  principalType?: string | undefined;
  /**
   * The unique identifier of the principal.
   */
  principalId?: string | undefined;
  /**
   * A list of all the roles that are added to this principal.
   */
  roles?: Array<EffectiveRole> | undefined;
};

/** @internal */
export const PrincipalRole$inboundSchema: z.ZodMiniType<
  PrincipalRole,
  unknown
> = z.pipe(
  z.object({
    principal_type: types.optional(types.string()),
    principal_id: types.optional(types.string()),
    roles: types.optional(z.array(EffectiveRole$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "principal_type": "principalType",
      "principal_id": "principalId",
    });
  }),
);

export function principalRoleFromJSON(
  jsonString: string,
): SafeParseResult<PrincipalRole, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PrincipalRole$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PrincipalRole' from JSON`,
  );
}
