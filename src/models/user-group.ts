/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";
import {
  UserGroupUser,
  UserGroupUser$inboundSchema,
} from "./user-group-user.js";

/**
 * User group.
 */
export type UserGroup = {
  /**
   * The user group's ID.
   */
  id?: string | undefined;
  /**
   * The user group's name.
   */
  name?: string | undefined;
  /**
   * A list of user group users.
   *
   * @remarks
   * Only returned in extended_details.
   */
  users?: Array<UserGroupUser> | undefined;
  /**
   * The date when the user group was created.
   *
   * @remarks
   * Only returned in extended_details.
   */
  createdAt?: Date | undefined;
  /**
   * The date when the user group was last updated.
   *
   * @remarks
   * Only returned in extended_details.
   */
  updatedAt?: Date | undefined;
};

/** @internal */
export const UserGroup$inboundSchema: z.ZodMiniType<UserGroup, unknown> = z
  .pipe(
    z.object({
      id: types.optional(types.string()),
      name: types.optional(types.string()),
      users: types.optional(z.array(UserGroupUser$inboundSchema)),
      created_at: types.optional(types.date()),
      updated_at: types.optional(types.date()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "updated_at": "updatedAt",
      });
    }),
  );

export function userGroupFromJSON(
  jsonString: string,
): SafeParseResult<UserGroup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UserGroup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UserGroup' from JSON`,
  );
}
