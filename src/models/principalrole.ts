/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { EffectiveRole, EffectiveRole$inboundSchema } from "./effectiverole.js";
import { SDKValidationError } from "./sdkvalidationerror.js";

export type PrincipalRole = {
  /**
   * The type of principal you want to retrieve roles or permission policies for.
   *
   * @remarks
   *
   * - user
   *
   * - group
   *
   * - apiKey
   *
   * - provisioningKey
   */
  principalType?: string | undefined;
  /**
   * The unique identifier of the principal.
   */
  principalId?: string | undefined;
  /**
   * A list of all the roles that are added to this principal.
   */
  roles?: Array<EffectiveRole> | undefined;
};

/** @internal */
export const PrincipalRole$inboundSchema: z.ZodType<
  PrincipalRole,
  z.ZodTypeDef,
  unknown
> = z.object({
  principal_type: z.string().optional(),
  principal_id: z.string().optional(),
  roles: z.array(EffectiveRole$inboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    "principal_type": "principalType",
    "principal_id": "principalId",
  });
});

export function principalRoleFromJSON(
  jsonString: string,
): SafeParseResult<PrincipalRole, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PrincipalRole$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PrincipalRole' from JSON`,
  );
}
